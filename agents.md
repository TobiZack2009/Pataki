We are using vite, html, tailwind, and js with jsdoc, with firebase as a db and for auth
The app is offline first, and if it fails to make any request, it falls back to using offline technologies such as using mock data.